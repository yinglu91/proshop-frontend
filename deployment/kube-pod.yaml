apiVersion: v1
kind: Pod
metadata:
  name: proshop-frontend
  namespace: dev-01
spec:
  containers:
    - name: proshop-frontend
      image: yinglu91/proshop-frontend:1.0
      resources:
        limits:
          memory: '200Mi'
        requests:
          memory: '100Mi'
